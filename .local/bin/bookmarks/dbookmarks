#!/bin/bash

browser=${1:-brave-browser}
# Import the colors
. "${HOME}/.cache/wal/colors.sh"

$browser $(cat ~/.local/bin/bookmarks/list | dmenu -nb "$color0" -nf "$color15" -sb "$color1" -sf "$color15" -i -l 20 | awk -F'|' '{gsub(/ /, "", $2); print $2}')
