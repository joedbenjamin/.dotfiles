#!/bin/zsh
#

# wal -i ~/Pictures -qs
# feh --bg-fill "$(< "${HOME}/.cache/wal/wal")" 
# feh --bg-fill ~/Pictures/rainDropsLeaf.jpg

while true; do
  wal -i ~/Pictures -qs
  feh --bg-fill "$(< "${HOME}/.cache/wal/wal")" 
  # feh --randomize --bg-fill ~/Pictures/*
  sleep 10m
done
